import MedibillApp from "@/components/MedibillApp";

/**
 * Página principal de Medibill — Server Component.
 * Toda la lógica interactiva vive en MedibillApp (Client Island).
 */
export default function MedibillHome() {
  return <MedibillApp />;
}