{
  "name": "muv-service",
  "version": "1.0.0",
  "private": true,
  "description": "MUV validation service for Cloud Run — validates FEV XML + RIPS JSON against MinSalud",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  }
}
